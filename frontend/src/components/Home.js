import React from 'react';

const Home = () => {
  const logIn = () => {
    // Log in actions
  };

  return (
    <div>
      <h1>Spotify Lyrics</h1>
      
      <button onClick={() => logIn()}>Log In</button>
    </div>
  );
};

export default Home;